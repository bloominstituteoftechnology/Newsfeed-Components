
const toggleMenu = () => {
  // Toggle the "menu--open" class on your menu refence. 
}

// Start Here: Create a reference to the ".menu" class
const menu = menu.document.querySelector('.menu');


// create a reference to the ".menu-button" class
const menuButton = menuButton.document.querySelector('.menu')

// Using your menuButton reference, add a click handler that calls toggleMenu
menuButton.addEventListener('click', function(element){
  this.element.addEventListener('click', () => { this.buttonClick() });
})